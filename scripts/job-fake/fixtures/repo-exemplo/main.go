// Arquivo de conteúdo do repo fixture: existe para o checkout ter o que materializar e para o
// `git rev-parse HEAD` do harness apontar para um commit de verdade.
package main

import "fmt"

func main() {
	fmt.Println("job fake da F2")
}
