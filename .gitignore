# dependências
node_modules/
.pnpm-store/

# build
dist/
build/
*.tsbuildinfo

# client do Prisma: código gerado a partir de apps/api/prisma/schema.prisma.
# Nasce com `pnpm db:generate` (que `pnpm db:migrate` já encadeia).
apps/api/generated/

# segredos e ambiente local
.env
.env.*
!.env.example

# artefatos de execução do sistema
banca-jobs/
backups/
*.log

# settings locais do Claude Code (o settings.json do projeto é versionado)
.claude/settings.local.json

# editores e SO
.vscode/
.idea/
.DS_Store
